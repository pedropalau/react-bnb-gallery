# Legacy Example App (Removed)

The standalone Create React App example was removed as part of the modernization work.

Use the unified docs/demo app instead:

```bash
pnpm docs:dev
```

The production docs/demo is published at:

- https://peterpalau.github.io/react-bnb-gallery/
